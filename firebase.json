{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": [{
    "target":"mobile",
    "public": "build",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }]
}